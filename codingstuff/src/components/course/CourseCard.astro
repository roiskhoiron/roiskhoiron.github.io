---
import { getChapterCount } from "@/lib/course";
import { formatDate, getCategoryColor } from "@/lib/utils";
import type { StructureItemType } from "@/schemas/content/course";

export interface CourseCardProps {
  id: string;
  title: string;
  description: string;
  author?: string;
  pubDate: Date;
  cover?: string;
  tags?: string[];
  structure: StructureItemType[];
}

interface Props {
  course: CourseCardProps;
}

const { course } = Astro.props;
const courseSlug = course.id.replace(/\/toc$/, "");
const chapterCount = getChapterCount(course.structure);
---

<div
  class="relative overflow-hidden bg-card border-none pt-0 gap-0 w-full flex flex-col cursor-pointer transition-all duration-200 hover:-translate-y-1 hover:shadow-xl rounded-2xl shadow-lg"
>
  <a href={`/courses/${courseSlug}`} class="flex flex-col grow">
    <header class="p-0 gap-0">
      <div class="aspect-video w-full bg-muted relative overflow-hidden">
        {
          course.cover ? (
            <img
              src={course.cover}
              alt={course.title}
              class="w-full h-full object-cover"
            />
          ) : (
            <div class="flex h-full items-center justify-center">
              <div
                class={`h-full w-full bg-linear-to-br ${getCategoryColor(course.id)} flex items-center justify-center`}
              >
                <svg
                  class="w-16 h-16 text-muted-foreground/30"
                  fill="none"
                  stroke="currentColor"
                  viewBox="0 0 24 24"
                >
                  <path
                    stroke-linecap="round"
                    stroke-linejoin="round"
                    stroke-width="1.5"
                    d="M12 6.253v13m0-13C10.832 5.477 9.246 5 7.5 5S4.168 5.477 3 6.253v13C4.168 18.477 5.754 18 7.5 18s3.332.477 4.5 1.253m0-13C13.168 5.477 14.754 5 16.5 5c1.747 0 3.332.477 4.5 1.253v13C19.832 18.477 18.247 18 16.5 18c-1.746 0-3.332.477-4.5 1.253"
                  />
                </svg>
              </div>
            </div>
          )
        }
      </div>
    </header>
    <div class="p-5">
      <h3
        class="line-clamp-2 font-heading text-lg text-foreground font-bold"
      >
        {course.title}
      </h3>
      <p class="mt-2 line-clamp-2 text-sm text-muted-foreground">
        {course.description}
      </p>
    </div>
    <footer class="px-5 pb-4">
      <div class="flex items-center justify-between text-xs text-muted-foreground">
        <span>{chapterCount} chapters</span>
        <span>{formatDate(course.pubDate)}</span>
      </div>
    </footer>
  </a>
  <div class="flex items-center justify-between px-5 pb-5 pt-0">
    <a
      href={`/courses/${courseSlug}`}
      class="text-xs font-semibold text-primary hover:text-primary/80 transition-colors"
    >
      Start Learning →
    </a>
    {
      course.author && (
        <span class="text-xs text-muted-foreground">{course.author}</span>
      )
    }
  </div>
</div>
